{
  "$defs": {
    "AddBoundaryConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        }
      },
      "title": "AddBoundaryConfig",
      "type": "object"
    },
    "ChaoticConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 2.0,
          "description": "Spacing between the coarse baseline segments.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        },
        "depth": {
          "default": 4,
          "description": "Recursion depth for the fractal generation.",
          "minimum": 0,
          "title": "Depth",
          "type": "integer"
        },
        "chaos_freq": {
          "default": 0.15,
          "description": "Spatial frequency of the chaotic distortion.",
          "title": "Chaos Freq",
          "type": "number"
        },
        "chaos_amp": {
          "default": 0.8,
          "description": "Amplitude of the chaotic distortion.",
          "title": "Chaos Amp",
          "type": "number"
        }
      },
      "title": "ChaoticConfig",
      "type": "object"
    },
    "ChladniConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "n": {
          "default": 3.0,
          "description": "Horizontal resonant mode.",
          "title": "N",
          "type": "number"
        },
        "m": {
          "default": 5.0,
          "description": "Vertical resonant mode.",
          "title": "M",
          "type": "number"
        },
        "sign": {
          "default": -1.0,
          "description": "Sign of the wave interference.",
          "title": "Sign",
          "type": "number"
        },
        "res": {
          "default": 0.5,
          "description": "Grid sampling resolution.",
          "exclusiveMinimum": 0.0,
          "title": "Res",
          "type": "number"
        },
        "simplify": {
          "default": 0.0,
          "description": "Tolerance for path simplification.",
          "minimum": 0.0,
          "title": "Simplify",
          "type": "number"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional image modulator.",
          "title": "Image Path"
        }
      },
      "title": "ChladniConfig",
      "type": "object"
    },
    "ClipConfig": {
      "properties": {},
      "title": "ClipConfig",
      "type": "object"
    },
    "ConcentricConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "center_x": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "X coordinate of the pattern center. Defaults to centroid.",
          "title": "Center X"
        },
        "center_y": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Y coordinate of the pattern center. Defaults to centroid.",
          "title": "Center Y"
        },
        "group_boundaries": {
          "default": false,
          "description": "If true, generates a single pattern globally centered across all boundaries.",
          "title": "Group Boundaries",
          "type": "boolean"
        },
        "min_radius": {
          "default": 5.0,
          "description": "Radius of the innermost shape.",
          "title": "Min Radius",
          "type": "number"
        },
        "max_radius": {
          "default": 100.0,
          "description": "Radius of the outermost shape.",
          "title": "Max Radius",
          "type": "number"
        },
        "spacing": {
          "default": 5.0,
          "description": "Distance between consecutive concentric rings.",
          "title": "Spacing",
          "type": "number"
        },
        "sides": {
          "default": 64,
          "description": "Number of sides. Use high values (e.g., 64) for circles, or low values (e.g., 3, 4, 6) for polygons.",
          "minimum": 3,
          "title": "Sides",
          "type": "integer"
        },
        "rotation": {
          "default": 0.0,
          "description": "Rotation angle in degrees for the generated shapes.",
          "title": "Rotation",
          "type": "number"
        }
      },
      "title": "ConcentricConfig",
      "type": "object"
    },
    "ConnectContinuousConfig": {
      "properties": {
        "snap_distance": {
          "default": 0.05,
          "description": "Max distance to weld segments.",
          "title": "Snap Distance",
          "type": "number"
        }
      },
      "title": "ConnectContinuousConfig",
      "type": "object"
    },
    "DeduplicateConfig": {
      "properties": {
        "tolerance": {
          "default": 1e-05,
          "description": "Tolerance for exactly equal geometries.",
          "title": "Tolerance",
          "type": "number"
        }
      },
      "title": "DeduplicateConfig",
      "type": "object"
    },
    "FlowFieldConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 3.0,
          "description": "Distance between initial seed points.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        },
        "step_length": {
          "default": 1.0,
          "description": "Distance path travels in single step.",
          "exclusiveMinimum": 0.0,
          "title": "Step Length",
          "type": "number"
        },
        "max_steps": {
          "default": 50,
          "description": "Maximum number of steps.",
          "exclusiveMinimum": 0,
          "title": "Max Steps",
          "type": "integer"
        },
        "scale": {
          "default": 0.1,
          "description": "Scaling factor for vector field math.",
          "title": "Scale",
          "type": "number"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "title": "Seed",
          "type": "integer"
        }
      },
      "title": "FlowFieldConfig",
      "type": "object"
    },
    "GenerateInCellsConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "generator": {
          "description": "The registry name of the generator to run in each cell.",
          "title": "Generator",
          "type": "string"
        },
        "generator_settings": {
          "additionalProperties": true,
          "description": "Settings to pass to the sub-generator.",
          "title": "Generator Settings",
          "type": "object"
        },
        "auto_center": {
          "default": true,
          "description": "Automatically inject center_x and center_y for the sub-generator based on cell centroid.",
          "title": "Auto Center",
          "type": "boolean"
        },
        "auto_rotate": {
          "default": false,
          "description": "Automatically calculate the minimum rotated rectangle of the cell and inject its angle.",
          "title": "Auto Rotate",
          "type": "boolean"
        },
        "rotation_setting": {
          "default": "rotation",
          "description": "The parameter name in the sub-generator to inject the calculated angle into (e.g., 'rotation').",
          "title": "Rotation Setting",
          "type": "string"
        },
        "keep_scaffolding": {
          "default": false,
          "description": "If true, includes the original incoming lines (the grid) in the final output.",
          "title": "Keep Scaffolding",
          "type": "boolean"
        },
        "tolerance": {
          "default": 1e-05,
          "description": "Grid size for snapping vertices to resolve floating-point gaps. Set to 0 to disable.",
          "title": "Tolerance",
          "type": "number"
        },
        "image_modulator": {
          "anyOf": [
            {
              "$ref": "#/$defs/ImageModulatorConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional configuration to dynamically modulate a setting based on an image."
        }
      },
      "required": [
        "generator"
      ],
      "title": "GenerateInCellsConfig",
      "type": "object"
    },
    "GridLinesConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 0.5,
          "description": "Distance between consecutive lines.",
          "title": "Spacing",
          "type": "number"
        },
        "orientation": {
          "default": "horizontal",
          "description": "Orientation of lines: 'horizontal', 'vertical', or 'crosshatch'.",
          "title": "Orientation",
          "type": "string"
        }
      },
      "title": "GridLinesConfig",
      "type": "object"
    },
    "GuillocheConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "center_x": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "X coordinate of the pattern center. Defaults to centroid.",
          "title": "Center X"
        },
        "center_y": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Y coordinate of the pattern center. Defaults to centroid.",
          "title": "Center Y"
        },
        "group_boundaries": {
          "default": false,
          "description": "If true, generates a single pattern globally centered across all boundaries.",
          "title": "Group Boundaries",
          "type": "boolean"
        },
        "R": {
          "default": 50.0,
          "description": "Radius of the fixed outer circle.",
          "title": "R",
          "type": "number"
        },
        "r": {
          "default": 35.0,
          "description": "Radius of the rolling inner circle.",
          "title": "R",
          "type": "number"
        },
        "p": {
          "default": 25.0,
          "description": "Distance from the pen to the center of the rolling circle.",
          "title": "P",
          "type": "number"
        },
        "steps": {
          "default": 1000,
          "description": "Total number of linear steps/points sampled along the curve.",
          "title": "Steps",
          "type": "integer"
        },
        "revolutions": {
          "default": 10.0,
          "description": "Number of full outer loops ($2\\pi$ rotations) to complete.",
          "title": "Revolutions",
          "type": "number"
        },
        "amplitude_mod": {
          "default": 0.0,
          "description": "Amplitude of secondary wave modulation.",
          "title": "Amplitude Mod",
          "type": "number"
        },
        "frequency_mod": {
          "default": 0.0,
          "description": "Frequency multiplier of secondary wave modulation.",
          "title": "Frequency Mod",
          "type": "number"
        }
      },
      "title": "GuillocheConfig",
      "type": "object"
    },
    "HexagonConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "radius": {
          "default": 3.0,
          "description": "Outer radius of a single hexagon cell.",
          "exclusiveMinimum": 0.0,
          "title": "Radius",
          "type": "number"
        }
      },
      "title": "HexagonConfig",
      "type": "object"
    },
    "HilbertConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 2.0,
          "description": "Target distance between segments.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        }
      },
      "title": "HilbertConfig",
      "type": "object"
    },
    "ImageMaskConfig": {
      "properties": {
        "mask_image": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Mask Image"
        },
        "threshold": {
          "default": 0.5,
          "description": "Darkness threshold (0.0 to 1.0) to keep lines.",
          "title": "Threshold",
          "type": "number"
        },
        "sample_step": {
          "default": 0.5,
          "description": "Resolution step size for sampling.",
          "title": "Sample Step",
          "type": "number"
        }
      },
      "title": "ImageMaskConfig",
      "type": "object"
    },
    "ImageModulatorConfig": {
      "properties": {
        "image_path": {
          "description": "File path to the source image to sample.",
          "title": "Image Path",
          "type": "string"
        },
        "target_setting": {
          "description": "The parameter name in the sub-generator to modulate (e.g., 'revolutions', 'spacing').",
          "title": "Target Setting",
          "type": "string"
        },
        "min_val": {
          "description": "The setting value when the image is pure white (0% darkness).",
          "title": "Min Val",
          "type": "number"
        },
        "max_val": {
          "description": "The setting value when the image is pure black (100% darkness).",
          "title": "Max Val",
          "type": "number"
        }
      },
      "required": [
        "image_path",
        "target_setting",
        "min_val",
        "max_val"
      ],
      "title": "ImageModulatorConfig",
      "type": "object"
    },
    "ImageOutlineConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "File path to the source image.",
          "title": "Image Path"
        },
        "resolution": {
          "default": 0.5,
          "description": "Sampling grid resolution.",
          "exclusiveMinimum": 0.0,
          "title": "Resolution",
          "type": "number"
        },
        "sigma": {
          "default": 1.0,
          "description": "Blur factor to smooth out noise.",
          "minimum": 0.0,
          "title": "Sigma",
          "type": "number"
        },
        "min_length": {
          "default": 2.0,
          "description": "Minimum physical length to keep.",
          "minimum": 0.0,
          "title": "Min Length",
          "type": "number"
        }
      },
      "title": "ImageOutlineConfig",
      "type": "object"
    },
    "LichtenbergConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "center_x": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "X coordinate of the pattern center. Defaults to centroid.",
          "title": "Center X"
        },
        "center_y": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Y coordinate of the pattern center. Defaults to centroid.",
          "title": "Center Y"
        },
        "group_boundaries": {
          "default": false,
          "description": "If true, generates a single pattern globally centered across all boundaries.",
          "title": "Group Boundaries",
          "type": "boolean"
        },
        "nodes": {
          "default": 1500,
          "description": "Target number of branches/nodes to generate per region.",
          "exclusiveMinimum": 0,
          "title": "Nodes",
          "type": "integer"
        },
        "spacing": {
          "default": 2.0,
          "description": "Length of each branching segment.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for reproducible fractal generation.",
          "title": "Seed",
          "type": "integer"
        }
      },
      "title": "LichtenbergConfig",
      "type": "object"
    },
    "MergeConfig": {
      "properties": {
        "precision": {
          "default": 0.0,
          "description": "Grid precision for unioning (0.0 for default).",
          "title": "Precision",
          "type": "number"
        }
      },
      "title": "MergeConfig",
      "type": "object"
    },
    "OffsetFillConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 2.0,
          "description": "Distance between offset rings.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        },
        "ring_simplify": {
          "default": 0.2,
          "description": "Simplification tolerance.",
          "minimum": 0.0,
          "title": "Ring Simplify",
          "type": "number"
        }
      },
      "title": "OffsetFillConfig",
      "type": "object"
    },
    "OptimizeConfig": {
      "properties": {
        "start_x": {
          "default": 0.0,
          "description": "X coordinate to start optimizing from.",
          "title": "Start X",
          "type": "number"
        },
        "start_y": {
          "default": 0.0,
          "description": "Y coordinate to start optimizing from.",
          "title": "Start Y",
          "type": "number"
        }
      },
      "title": "OptimizeConfig",
      "type": "object"
    },
    "PeanoConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 2.0,
          "description": "Target spacing between lines.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        }
      },
      "title": "PeanoConfig",
      "type": "object"
    },
    "PenroseConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "center_x": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "X coordinate of the pattern center. Defaults to centroid.",
          "title": "Center X"
        },
        "center_y": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Y coordinate of the pattern center. Defaults to centroid.",
          "title": "Center Y"
        },
        "group_boundaries": {
          "default": false,
          "description": "If true, generates a single pattern globally centered across all boundaries.",
          "title": "Group Boundaries",
          "type": "boolean"
        },
        "depth": {
          "default": 5,
          "description": "Number of recursive deflation iterations. High values exponentially increase detail.",
          "minimum": 1,
          "title": "Depth",
          "type": "integer"
        }
      },
      "title": "PenroseConfig",
      "type": "object"
    },
    "PhotoConcentricConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 2.0,
          "description": "Distance between contour lines.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        },
        "threshold": {
          "default": 0.5,
          "description": "Darkness threshold (0.0-1.0).",
          "title": "Threshold",
          "type": "number"
        },
        "resolution": {
          "default": 0.25,
          "description": "Sampling grid resolution.",
          "exclusiveMinimum": 0.0,
          "title": "Resolution",
          "type": "number"
        },
        "min_length": {
          "default": 2.0,
          "description": "Minimum length to keep.",
          "minimum": 0.0,
          "title": "Min Length",
          "type": "number"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "File path to the source image.",
          "title": "Image Path"
        }
      },
      "title": "PhotoConcentricConfig",
      "type": "object"
    },
    "PhotoContourConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "levels": {
          "default": 15,
          "description": "Number of contour levels to generate.",
          "exclusiveMinimum": 0,
          "title": "Levels",
          "type": "integer"
        },
        "resolution": {
          "default": 0.5,
          "description": "Sampling grid resolution.",
          "exclusiveMinimum": 0.0,
          "title": "Resolution",
          "type": "number"
        },
        "min_length": {
          "default": 2.0,
          "description": "Minimum physical length to keep.",
          "minimum": 0.0,
          "title": "Min Length",
          "type": "number"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "File path to the source image.",
          "title": "Image Path"
        }
      },
      "title": "PhotoContourConfig",
      "type": "object"
    },
    "PhotoStippleConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "dots": {
          "default": 5000,
          "description": "Target number of stipple dots to generate.",
          "exclusiveMinimum": 0,
          "title": "Dots",
          "type": "integer"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Source image.",
          "title": "Image Path"
        },
        "dot_size": {
          "default": 0.1,
          "description": "Length of microscopic line.",
          "exclusiveMinimum": 0.0,
          "title": "Dot Size",
          "type": "number"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "title": "Seed",
          "type": "integer"
        }
      },
      "title": "PhotoStippleConfig",
      "type": "object"
    },
    "PhotoTSPConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "nodes": {
          "default": 2000,
          "description": "Number of points to connect.",
          "exclusiveMinimum": 1,
          "title": "Nodes",
          "type": "integer"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "File path to the source image.",
          "title": "Image Path"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "title": "Seed",
          "type": "integer"
        }
      },
      "title": "PhotoTSPConfig",
      "type": "object"
    },
    "PhotoWaveConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "lines": {
          "default": 80,
          "description": "Number of horizontal wave lines.",
          "exclusiveMinimum": 0,
          "title": "Lines",
          "type": "integer"
        },
        "amp": {
          "default": 2.0,
          "description": "Amplitude multiplier.",
          "title": "Amp",
          "type": "number"
        },
        "image_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "File path to the source image.",
          "title": "Image Path"
        }
      },
      "title": "PhotoWaveConfig",
      "type": "object"
    },
    "PolygonizeConfig": {
      "properties": {},
      "title": "PolygonizeConfig",
      "type": "object"
    },
    "ScaleConfig": {
      "properties": {
        "factor": {
          "default": 1.0,
          "description": "Uniform scaling multiplier.",
          "title": "Factor",
          "type": "number"
        },
        "origin": {
          "default": "center",
          "description": "Origin point for scaling ('center', 'centroid', etc).",
          "title": "Origin",
          "type": "string"
        }
      },
      "title": "ScaleConfig",
      "type": "object"
    },
    "SimplifyConfig": {
      "properties": {
        "tolerance": {
          "default": 0.1,
          "description": "Max allowed distance between original and simplified line.",
          "minimum": 0.0,
          "title": "Tolerance",
          "type": "number"
        },
        "preserve_topology": {
          "default": false,
          "description": "Prevents invalid geometries.",
          "title": "Preserve Topology",
          "type": "boolean"
        }
      },
      "title": "SimplifyConfig",
      "type": "object"
    },
    "SpiralConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "center_x": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "X coordinate of the pattern center. Defaults to centroid.",
          "title": "Center X"
        },
        "center_y": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Y coordinate of the pattern center. Defaults to centroid.",
          "title": "Center Y"
        },
        "group_boundaries": {
          "default": false,
          "description": "If true, generates a single pattern globally centered across all boundaries.",
          "title": "Group Boundaries",
          "type": "boolean"
        },
        "start_radius": {
          "default": 0.0,
          "description": "Starting radius (can be > 0 for a hollow center).",
          "title": "Start Radius",
          "type": "number"
        },
        "end_radius": {
          "default": 100.0,
          "description": "Outer radius where the spiral terminates.",
          "title": "End Radius",
          "type": "number"
        },
        "revolutions": {
          "default": 10.0,
          "description": "Total number of full rotations.",
          "title": "Revolutions",
          "type": "number"
        },
        "steps": {
          "default": 1000,
          "description": "Total number of linear segments used to draw the curve.",
          "title": "Steps",
          "type": "integer"
        }
      },
      "title": "SpiralConfig",
      "type": "object"
    },
    "Step_add_boundary": {
      "properties": {
        "operation": {
          "const": "add_boundary",
          "description": "Execute the 'add_boundary' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/AddBoundaryConfig",
          "description": "Settings for 'add_boundary'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_add_boundary",
      "type": "object"
    },
    "Step_chaotic": {
      "properties": {
        "operation": {
          "const": "chaotic",
          "description": "Execute the 'chaotic' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ChaoticConfig",
          "description": "Settings for 'chaotic'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_chaotic",
      "type": "object"
    },
    "Step_chladni": {
      "properties": {
        "operation": {
          "const": "chladni",
          "description": "Execute the 'chladni' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ChladniConfig",
          "description": "Settings for 'chladni'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_chladni",
      "type": "object"
    },
    "Step_clip": {
      "properties": {
        "operation": {
          "const": "clip",
          "description": "Execute the 'clip' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ClipConfig",
          "description": "Settings for 'clip'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_clip",
      "type": "object"
    },
    "Step_concentric": {
      "properties": {
        "operation": {
          "const": "concentric",
          "description": "Execute the 'concentric' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ConcentricConfig",
          "description": "Settings for 'concentric'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_concentric",
      "type": "object"
    },
    "Step_connect_continuous": {
      "properties": {
        "operation": {
          "const": "connect_continuous",
          "description": "Execute the 'connect_continuous' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ConnectContinuousConfig",
          "description": "Settings for 'connect_continuous'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_connect_continuous",
      "type": "object"
    },
    "Step_deduplicate": {
      "properties": {
        "operation": {
          "const": "deduplicate",
          "description": "Execute the 'deduplicate' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/DeduplicateConfig",
          "description": "Settings for 'deduplicate'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_deduplicate",
      "type": "object"
    },
    "Step_flow_field": {
      "properties": {
        "operation": {
          "const": "flow_field",
          "description": "Execute the 'flow_field' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/FlowFieldConfig",
          "description": "Settings for 'flow_field'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_flow_field",
      "type": "object"
    },
    "Step_generate_in_cells": {
      "properties": {
        "operation": {
          "const": "generate_in_cells",
          "description": "Execute the 'generate_in_cells' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/GenerateInCellsConfig",
          "description": "Settings for 'generate_in_cells'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_generate_in_cells",
      "type": "object"
    },
    "Step_grid_lines": {
      "properties": {
        "operation": {
          "const": "grid_lines",
          "description": "Execute the 'grid_lines' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/GridLinesConfig",
          "description": "Settings for 'grid_lines'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_grid_lines",
      "type": "object"
    },
    "Step_guilloche": {
      "properties": {
        "operation": {
          "const": "guilloche",
          "description": "Execute the 'guilloche' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/GuillocheConfig",
          "description": "Settings for 'guilloche'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_guilloche",
      "type": "object"
    },
    "Step_hexagon": {
      "properties": {
        "operation": {
          "const": "hexagon",
          "description": "Execute the 'hexagon' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/HexagonConfig",
          "description": "Settings for 'hexagon'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_hexagon",
      "type": "object"
    },
    "Step_hilbert": {
      "properties": {
        "operation": {
          "const": "hilbert",
          "description": "Execute the 'hilbert' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/HilbertConfig",
          "description": "Settings for 'hilbert'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_hilbert",
      "type": "object"
    },
    "Step_image_mask": {
      "properties": {
        "operation": {
          "const": "image_mask",
          "description": "Execute the 'image_mask' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ImageMaskConfig",
          "description": "Settings for 'image_mask'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_image_mask",
      "type": "object"
    },
    "Step_image_outline": {
      "properties": {
        "operation": {
          "const": "image_outline",
          "description": "Execute the 'image_outline' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ImageOutlineConfig",
          "description": "Settings for 'image_outline'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_image_outline",
      "type": "object"
    },
    "Step_lichtenberg": {
      "properties": {
        "operation": {
          "const": "lichtenberg",
          "description": "Execute the 'lichtenberg' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/LichtenbergConfig",
          "description": "Settings for 'lichtenberg'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_lichtenberg",
      "type": "object"
    },
    "Step_merge": {
      "properties": {
        "operation": {
          "const": "merge",
          "description": "Execute the 'merge' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/MergeConfig",
          "description": "Settings for 'merge'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_merge",
      "type": "object"
    },
    "Step_offset_fill": {
      "properties": {
        "operation": {
          "const": "offset_fill",
          "description": "Execute the 'offset_fill' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/OffsetFillConfig",
          "description": "Settings for 'offset_fill'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_offset_fill",
      "type": "object"
    },
    "Step_optimize": {
      "properties": {
        "operation": {
          "const": "optimize",
          "description": "Execute the 'optimize' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/OptimizeConfig",
          "description": "Settings for 'optimize'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_optimize",
      "type": "object"
    },
    "Step_peano": {
      "properties": {
        "operation": {
          "const": "peano",
          "description": "Execute the 'peano' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PeanoConfig",
          "description": "Settings for 'peano'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_peano",
      "type": "object"
    },
    "Step_penrose": {
      "properties": {
        "operation": {
          "const": "penrose",
          "description": "Execute the 'penrose' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PenroseConfig",
          "description": "Settings for 'penrose'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_penrose",
      "type": "object"
    },
    "Step_photo_concentric": {
      "properties": {
        "operation": {
          "const": "photo_concentric",
          "description": "Execute the 'photo_concentric' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PhotoConcentricConfig",
          "description": "Settings for 'photo_concentric'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_photo_concentric",
      "type": "object"
    },
    "Step_photo_contour": {
      "properties": {
        "operation": {
          "const": "photo_contour",
          "description": "Execute the 'photo_contour' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PhotoContourConfig",
          "description": "Settings for 'photo_contour'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_photo_contour",
      "type": "object"
    },
    "Step_photo_stipple": {
      "properties": {
        "operation": {
          "const": "photo_stipple",
          "description": "Execute the 'photo_stipple' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PhotoStippleConfig",
          "description": "Settings for 'photo_stipple'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_photo_stipple",
      "type": "object"
    },
    "Step_photo_tsp": {
      "properties": {
        "operation": {
          "const": "photo_tsp",
          "description": "Execute the 'photo_tsp' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PhotoTSPConfig",
          "description": "Settings for 'photo_tsp'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_photo_tsp",
      "type": "object"
    },
    "Step_photo_wave": {
      "properties": {
        "operation": {
          "const": "photo_wave",
          "description": "Execute the 'photo_wave' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PhotoWaveConfig",
          "description": "Settings for 'photo_wave'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_photo_wave",
      "type": "object"
    },
    "Step_polygonize": {
      "properties": {
        "operation": {
          "const": "polygonize",
          "description": "Execute the 'polygonize' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/PolygonizeConfig",
          "description": "Settings for 'polygonize'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_polygonize",
      "type": "object"
    },
    "Step_scale": {
      "properties": {
        "operation": {
          "const": "scale",
          "description": "Execute the 'scale' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ScaleConfig",
          "description": "Settings for 'scale'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_scale",
      "type": "object"
    },
    "Step_simplify": {
      "properties": {
        "operation": {
          "const": "simplify",
          "description": "Execute the 'simplify' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/SimplifyConfig",
          "description": "Settings for 'simplify'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_simplify",
      "type": "object"
    },
    "Step_spiral": {
      "properties": {
        "operation": {
          "const": "spiral",
          "description": "Execute the 'spiral' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/SpiralConfig",
          "description": "Settings for 'spiral'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_spiral",
      "type": "object"
    },
    "Step_triangle": {
      "properties": {
        "operation": {
          "const": "triangle",
          "description": "Execute the 'triangle' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/TriangleConfig",
          "description": "Settings for 'triangle'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_triangle",
      "type": "object"
    },
    "Step_venation": {
      "properties": {
        "operation": {
          "const": "venation",
          "description": "Execute the 'venation' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/VenationConfig",
          "description": "Settings for 'venation'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_venation",
      "type": "object"
    },
    "Step_voronoi_dual": {
      "properties": {
        "operation": {
          "const": "voronoi_dual",
          "description": "Execute the 'voronoi_dual' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/VoronoiDualConfig",
          "description": "Settings for 'voronoi_dual'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_voronoi_dual",
      "type": "object"
    },
    "Step_zoom": {
      "properties": {
        "operation": {
          "const": "zoom",
          "description": "Execute the 'zoom' operation.",
          "title": "Operation",
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/ZoomConfig",
          "description": "Settings for 'zoom'."
        }
      },
      "required": [
        "operation"
      ],
      "title": "Step_zoom",
      "type": "object"
    },
    "TriangleConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "cell_size": {
          "default": 5.0,
          "description": "Side length of equilateral triangles.",
          "exclusiveMinimum": 0.0,
          "title": "Cell Size",
          "type": "number"
        }
      },
      "title": "TriangleConfig",
      "type": "object"
    },
    "VenationConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "center_x": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "X coordinate of the pattern center. Defaults to centroid.",
          "title": "Center X"
        },
        "center_y": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Y coordinate of the pattern center. Defaults to centroid.",
          "title": "Center Y"
        },
        "group_boundaries": {
          "default": false,
          "description": "If true, generates a single pattern globally centered across all boundaries.",
          "title": "Group Boundaries",
          "type": "boolean"
        },
        "num_leaves": {
          "default": 500,
          "description": "Number of attractor points (leaves) to drive the growth.",
          "title": "Num Leaves",
          "type": "integer"
        },
        "kill_distance": {
          "default": 2.0,
          "description": "Distance at which an attractor is considered reached and removed.",
          "title": "Kill Distance",
          "type": "number"
        },
        "attract_distance": {
          "default": 20.0,
          "description": "Maximum distance an attractor can influence a growing vein.",
          "title": "Attract Distance",
          "type": "number"
        },
        "segment_length": {
          "default": 1.0,
          "description": "How far a vein grows in a single step.",
          "title": "Segment Length",
          "type": "number"
        },
        "root_x": {
          "default": 100.0,
          "description": "Starting X coordinate of the root vein.",
          "title": "Root X",
          "type": "number"
        },
        "root_y": {
          "default": 0.0,
          "description": "Starting Y coordinate of the root vein.",
          "title": "Root Y",
          "type": "number"
        },
        "seed": {
          "default": 42,
          "description": "Random seed for repeatable patterns.",
          "title": "Seed",
          "type": "integer"
        },
        "max_iterations": {
          "default": 1000,
          "description": "Safety limit to prevent infinite generation loops.",
          "title": "Max Iterations",
          "type": "integer"
        }
      },
      "title": "VenationConfig",
      "type": "object"
    },
    "VoronoiDualConfig": {
      "properties": {
        "overscan": {
          "default": 0.0,
          "description": "Distance to expand (positive) or shrink (negative) the operation's clipping boundary.",
          "title": "Overscan",
          "type": "number"
        },
        "spacing": {
          "default": 2.0,
          "description": "Target spacing between points.",
          "exclusiveMinimum": 0.0,
          "title": "Spacing",
          "type": "number"
        },
        "num_points": {
          "default": 0,
          "description": "Number of random seed points.",
          "minimum": 0,
          "title": "Num Points",
          "type": "integer"
        },
        "seed": {
          "default": 42,
          "description": "Random seed.",
          "title": "Seed",
          "type": "integer"
        },
        "mode": {
          "default": "dual",
          "description": "Mode: 'voronoi', 'delaunay', or 'dual'.",
          "title": "Mode",
          "type": "string"
        }
      },
      "title": "VoronoiDualConfig",
      "type": "object"
    },
    "ZoomConfig": {
      "properties": {
        "factor": {
          "default": 1.0,
          "description": "Zoom multiplier.",
          "title": "Factor",
          "type": "number"
        },
        "origin": {
          "default": "center",
          "description": "Origin point for zoom.",
          "title": "Origin",
          "type": "string"
        }
      },
      "title": "ZoomConfig",
      "type": "object"
    }
  },
  "items": {
    "anyOf": [
      {
        "$ref": "#/$defs/Step_chaotic"
      },
      {
        "$ref": "#/$defs/Step_hexagon"
      },
      {
        "$ref": "#/$defs/Step_hilbert"
      },
      {
        "$ref": "#/$defs/Step_lichtenberg"
      },
      {
        "$ref": "#/$defs/Step_peano"
      },
      {
        "$ref": "#/$defs/Step_penrose"
      },
      {
        "$ref": "#/$defs/Step_photo_concentric"
      },
      {
        "$ref": "#/$defs/Step_photo_stipple"
      },
      {
        "$ref": "#/$defs/Step_photo_tsp"
      },
      {
        "$ref": "#/$defs/Step_triangle"
      },
      {
        "$ref": "#/$defs/Step_add_boundary"
      },
      {
        "$ref": "#/$defs/Step_grid_lines"
      },
      {
        "$ref": "#/$defs/Step_image_outline"
      },
      {
        "$ref": "#/$defs/Step_photo_contour"
      },
      {
        "$ref": "#/$defs/Step_photo_wave"
      },
      {
        "$ref": "#/$defs/Step_concentric"
      },
      {
        "$ref": "#/$defs/Step_spiral"
      },
      {
        "$ref": "#/$defs/Step_chladni"
      },
      {
        "$ref": "#/$defs/Step_flow_field"
      },
      {
        "$ref": "#/$defs/Step_guilloche"
      },
      {
        "$ref": "#/$defs/Step_offset_fill"
      },
      {
        "$ref": "#/$defs/Step_venation"
      },
      {
        "$ref": "#/$defs/Step_voronoi_dual"
      },
      {
        "$ref": "#/$defs/Step_clip"
      },
      {
        "$ref": "#/$defs/Step_deduplicate"
      },
      {
        "$ref": "#/$defs/Step_merge"
      },
      {
        "$ref": "#/$defs/Step_polygonize"
      },
      {
        "$ref": "#/$defs/Step_zoom"
      },
      {
        "$ref": "#/$defs/Step_connect_continuous"
      },
      {
        "$ref": "#/$defs/Step_optimize"
      },
      {
        "$ref": "#/$defs/Step_scale"
      },
      {
        "$ref": "#/$defs/Step_simplify"
      },
      {
        "$ref": "#/$defs/Step_generate_in_cells"
      },
      {
        "$ref": "#/$defs/Step_image_mask"
      }
    ]
  },
  "title": "RootModel[List[Union[Step_chaotic, Step_hexagon, Step_hilbert, Step_lichtenberg, Step_peano, Step_penrose, Step_photo_concentric, Step_photo_stipple, Step_photo_tsp, Step_triangle, Step_add_boundary, Step_grid_lines, Step_image_outline, Step_photo_contour, Step_photo_wave, Step_concentric, Step_spiral, Step_chladni, Step_flow_field, Step_guilloche, Step_offset_fill, Step_venation, Step_voronoi_dual, Step_clip, Step_deduplicate, Step_merge, Step_polygonize, Step_zoom, Step_connect_continuous, Step_optimize, Step_scale, Step_simplify, Step_generate_in_cells, Step_image_mask]]]",
  "type": "array"
}